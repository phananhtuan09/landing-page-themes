"use client";

/**
 * WebGL Canvas component - Currently disabled
 * The scroll-driven distortion effect was removed as it made content hard to read.
 * The component is kept as a placeholder for potential future use with different effects.
 */
export function WebGLCanvas() {
  // WebGL distortion effect disabled for better readability
  return null;
}
